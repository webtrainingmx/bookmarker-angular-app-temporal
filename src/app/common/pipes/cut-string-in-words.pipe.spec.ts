import { CutStringInWordsPipe } from './cut-string-in-words.pipe';

describe('CutStringInWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new CutStringInWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
